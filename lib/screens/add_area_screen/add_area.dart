import 'package:flutter/material.dart';

class AddArea extends StatelessWidget {
  const AddArea({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text("HELLLO WORLDD"),);
  }
}